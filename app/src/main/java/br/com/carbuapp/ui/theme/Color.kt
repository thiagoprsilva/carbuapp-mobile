package br.com.carbuapp.ui.theme

import androidx.compose.ui.graphics.Color

// =========================================================
// Paleta CarbuApp — extraída de /frontend/src/styles/global.css
// Mantém a mesma identidade visual da versão Web (tema escuro)
// =========================================================

// Fundos
val CarbuBg = Color(0xFF0A0A0C)        // --bg
val CarbuCard = Color(0xFF111115)      // --card
val CarbuCard2 = Color(0xFF17171D)     // --card2
val CarbuSurfaceAlt = Color(0xFF0F0F16) // inputs / fundos auxiliares
val CarbuSidebar = Color(0xFF0D0D12)   // gradiente sidebar / topo

// Texto
val CarbuText = Color(0xFFF0F0F4)      // --text
val CarbuMuted = Color(0xFF8B8D9E)     // --muted
val CarbuBorder = Color(0xFF242430)    // --border

// Marca (laranja)
val CarbuPrimary = Color(0xFFF97316)       // --primary
val CarbuPrimaryHover = Color(0xFFEA6C0A)  // --primaryHover
val CarbuPrimaryText = Color(0xFF0A0A0C)   // --primaryText (texto sobre laranja)

// Cores de apoio / status
val CarbuBlue = Color(0xFF60A5FA)      // --blue (Executado / info)
val CarbuRed = Color(0xFFF87171)       // --red (erro / Rejeitado)
val CarbuGreen = Color(0xFF4ADE80)     // sucesso / Aprovado
val CarbuYellow = Color(0xFFFBBF24)    // Pendente

// Botões neutros
val CarbuGrayBtn = Color(0xFF1E1E28)       // --grayBtn
val CarbuGrayBtnText = Color(0xFFD1D1E0)   // --grayBtnText
